Widen the body by 15mm and adjust the spacing of the button clusters accordingly. Also, update this controller to a left-handed variant by mirroring the layout of the buttons and triggers, but make sure that logos and standard parts are correct, not just a geometric flip.

The starting part: `input.SLDPRT`, with frozen baseline measurements in `input.json`.

Save your final edited part as `solution.SLDPRT`.
