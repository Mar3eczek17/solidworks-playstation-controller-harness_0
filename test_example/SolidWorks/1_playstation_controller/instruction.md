Widen the body by 15mm by increasing the separation between the two hand-grip halves (not by scaling the overall housing), and re-space the button clusters to match the wider stance. Convert the controller to a left-handed layout by swapping the D-pad and face-button clusters to the opposite side from where they currently sit, and swapping the L1/L2 and R1/R2 trigger/bumper assignments to match. The button clusters, START/SELECT labels, and shoulder buttons should end up in mirrored positions, but any text, logos, and standard/purchased parts (e.g. joystick caps) must remain legible and correctly oriented — a naive geometric mirror that flips the labels backwards or reverses standard-part geometry is incorrect.

The starting part: `input.SLDPRT`, with frozen baseline measurements in `input.json`.

Save your final edited part as `solution.SLDPRT`.
